---
title: Content rendering test page
description: This page is used for testing page rendering, and design elements for hexandcube.com. Also, this is a description of a page which appears directly below the title heading.
date: 2025-09-19
lastmod: 2026-02-15
category: Test category
hideReadingTime: false
noindex: true
draft: false
---

# This is a level 1 heading [H1]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

## This is a level 2 heading [H2]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

### This is a level 3 heading [H3]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

#### This is a level 4 heading [H4]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

##### This is a level 5 heading [H5]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

###### This is a level 6 heading [H6]

This is an example paragraph to demonstrate how different heading levels look alongside regular text.
If you want to decide what to do after you take each screenshot, deselect everything in `After capture tasks`, and
select then select `Show “After capture” window`. ShareX is very customizable, so you can also edit the options displayed in the 
`After capture` window. You can find more informaction about all the features of ShareX on https://getsharex.com/.

The upload path specifies where the files we upload through ShareX will be located. For example: If I use the path
`ShareX/%y/%mo`, my file will be uploaded to `ShareX/CurrentYear/CurrentMonth/filename.png` in my Scaleway bucket. We can
see the URL preview below. At the bottom of the settings page, make sure that the Storage class is set to `Amazon S3
Standard`, and that `Set public read ACL on file` is checked. This will make the files we upload through ShareX accessible
without authentication, so we can easily share them.

### Links

This is a [link to my website](https://hexandcube.com). And this is a link to [another website](https://vulpy.link).

And here's a clickable URL: <https://en.wikipedia.org/wiki/fox>

### Images

![Fox photo](https://api.tinyfox.dev/img?animal=fox "A fox photo from TinyFox's api")

### Emphasis

This text is in *italics*, or *emphasized*

This text is **bold**, or **strongly emphasized**

### Strikethrough

Hexandcube is ~~an arctic fox~~ a red fox

### Regular Emojis

These are regular unicode emojis, which appearance may vary depending on the platform and browser used.

:fox_face: :cat: :smile: :laughing: :blush: :smiley: :relaxed: :smirk: :heart_eyes: :kissing_heart:
:kissing_closed_eyes:
:flushed:

Here's an emoji rendered alongside some text :fox_face:

### Neofox Emojis

These are custom emojis from the [Neofox set](https://volpeon.ink/emojis/neofox) designed by [Volpeon](https://volpeon.ink).

{{< neofox snug >}} {{< neofox pat_snug>}} {{< neofox happy_blep >}} {{< neofox nom_egg >}}
{{< neofox nom_donut >}} {{< neofox nom_cookie >}} {{< neofox knife >}} {{< neofox knives >}}
{{< neofox gun >}} {{< neofox rainbow >}} {{< neofox blush_hide >}} {{< neofox owo >}} {{< neofox flop >}}

Here's a custom emoji rendered alongside some text {{< neofox happy_blep>}}

### Ordered Lists

1. Red fox
2. Fennec fox
3. Arctic fox
4. Gray fox
5. Kit fox

### Unordered Lists

- Bat eared fox
- Corsac fox
- Swift fox
- Tibetian fox
- Cape fox

### Task Lists

- [x] Cascade red fox (v. v. cascadensis)
- [x] European red fox (v. v. vulpes)
- [ ] American red fox (v. v. fulva)
- [ ] Ezo red fox (v. v. schrencki)
- [ ] Sierra Nevada red fox (v. v. necator)

### Tables

| Column 1 | Column 2 | Column 3 |
| -------- | -------- | -------- |
| Row 1    | Row 1    | Row 1    |
| Row 2    | Row 2    | Row 2    |
| Row 3    | Row 3    | Row 3    |

### Code Blocks

This is an `inline code block`.

Multiline Javascript example: 

```javascript
function deleteEntry(entryId) {
    // Deletes an entry from the editor and updates the IDs of the remaining entries
    const entries = document.getElementById("editor-entries");
    let entry = document.getElementById(`secretfileEditorEntry-${entryId}`);
    entry.remove();
    entryCounter--;

    entries.querySelectorAll(".secretfileEditorEntry").forEach((entry, i) => {
        entry.id = `secretfileEditorEntry-${i + 1}`;
        entry.querySelector("h3").innerHTML = `Entry ${i + 1}`;

        entry
            .querySelector(".entry-delete")
            .setAttribute("onclick", `deleteEntry(${i + 1})`);
    });

    updateEditor();
}
```

Multiline Java example:

```java
private static void renderToast(DrawContext context, TextRenderer textRenderer, int screenWidth, Text text, float progress) {
    float easedProgress = 1 - (1 - progress) * (1 - progress);
    int textWidth = textRenderer.getWidth(text);
    int textHeight = textRenderer.fontHeight;
    int padding = 6;
    int totalHeight = textHeight + (padding * 2);
    
    SignalLossConfig.ToastPosition pos = SignalLossConfig.INSTANCE.toastPosition;
    
    int x = switch(pos) {
        case LEFT -> 10;
        case CENTER -> (screenWidth - textWidth) / 2;
        case RIGHT -> screenWidth - textWidth - 10;
    };
    
    int hiddenY = -totalHeight - 5;
    int visibleY = 10;
    int y = (int) MathHelper.lerp(easedProgress, hiddenY, visibleY);
    
    if (SignalLossConfig.INSTANCE.drawBackground) {
        context.fill(x - padding, y - padding, x + textWidth + padding, y + textHeight + padding,
        SignalLossConfig.INSTANCE.backgroundColor);
    }
}

```

Multiline generic example:

```
Urban red foxes are most active at dusk and dawn, doing most of their hunting and scavenging at these times. It is
uncommon to spot them during the day, but they can be caught sunbathing on roofs of houses or sheds. Urban red foxes
will often make their homes in hidden and undisturbed spots in urban areas as well as on the edges of a city, visiting
at night for sustenance. They sleep at night in dens.

While urban red foxes will scavenge successfully in the city (and the red foxes tend to eat anything that humans eat)
some humans will deliberately leave food out for the animals, finding them endearing. Doing this regularly can attract
urban red foxes to one's home; they can become accustomed to human presence, warming up to their providers by allowing
themselves to be approached and in some cases even played with, particularly young kits.

    source: https://en.wikipedia.org/wiki/Red_fox, CC BY-SA 4.0
```

### Blockquotes

> Unlike humans, foxes don't build nuclear weapons...

### Horizontal rule

---

### Footnotes

Here's some text with a simple footnote[^1], and with a longer one[^bignote].

[^1]: This is the first footnote.

[^bignote]: Here's one with multiple paragraphs and code.

    Indented paragraphs will get included in the footnote.

    `Waff :3`

    EEEEEEEEEEE {{< neofox happy_blep>}}

### Abbreviations

<abbr title="HyperText Markup Language">HTML</abbr> & <abbr title="Cascading Style Sheets">CSS</abbr>

### Definition Lists

Term 1

: Definition 1
with lazy continuation.

Term 2 with *inline markup*

: Definition 2
`{ some code, part of Definition 2 }`

: Definition 3
Part of Term 2

### Buttons

###### Square buttons
{{< button href="https://hexandcube.com" square="true">}}<i data-lucide="download"></i>{{< /button>}}
{{< button href="https://hexandcube.com" square="true" secondary="true">}}<i data-lucide="download"></i>{{< /button>}}
{{< button href="https://hexandcube.com" square="true" tertiary="true">}}<i data-lucide="download"></i>{{< /button>}}

###### Small buttons
{{< button href="https://hexandcube.com" small="true" >}}Primary button{{< /button >}} 
{{< button href="https://hexandcube.com" secondary="true" small="true" >}}Secondary button{{< /button >}}
{{< button href="https://hexandcube.com" tertiary="true" small="true" >}}Tertiary button{{< /button >}}

###### Regular buttons
{{< button href="https://hexandcube.com" >}}Primary button{{< /button >}} 
{{< button href="https://hexandcube.com" secondary="true" >}}Secondary button{{< /button >}}
{{< button href="https://hexandcube.com" tertiary="true" >}}Tertiary button{{< /button >}}

###### Disabled buttons
{{< button href="https://hexandcube.com" disabled="true" >}}Primary button disabled{{< /button >}}
{{< button href="https://hexandcube.com" secondary="true" disabled="true" >}}Secondary button disabled{{< /button >}}

{{< button href="https://hexandcube.com" tertiary="true" disabled="true" >}}Tertiary button disabled{{< /button >}}

###### Full width buttons
{{< button href="https://hexandcube.com" full="true" >}}Example full width button{{< /button >}}